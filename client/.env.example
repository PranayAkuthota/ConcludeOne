# Base API URL (Optional if serving full-stack from the same Express instance on Render)
# If deploying frontend as a separate Render Static Site, set this to your backend service URL:
# VITE_API_URL=https://concludeone-backend.onrender.com
VITE_API_URL=
